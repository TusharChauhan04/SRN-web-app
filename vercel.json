{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "//regions": [
    "Run the functions NEXT TO THE DATABASE. Without this Vercel defaults to",
    "iad1 (Virginia) while Supabase is in ap-south-1 (Mumbai), so every query",
    "crosses the planet: X-Vercel-Id read `bom1::iad1::...` and a single",
    "healthz database ping cost 187ms, which is a Mumbai-Virginia round trip.",
    "The dashboard makes 13 round trips, so that alone was ~1s of pure network.",
    "If the database ever moves, move this with it — they must stay together."
  ],
  "regions": ["bom1"],
  "crons": [
    {
      "path": "/api/cron/gdpr-erasure",
      "schedule": "0 3 * * *"
    }
  ]
}
